---
title: "demo"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{demo}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(BTIPFlow)
library(tidyverse)
library(BTIPFlow)
library(flowCore)
library(flowWorkspace)
library(ggcyto)
library(cytotidyr)
```

## Demo

This is the original demo.

```{r Generate and Clean}
# fs <- generate_flowset(folder)
data(fs)
resQC <- clean_data(fs)
```

```{r}
comp <- compensate_data(comp_controls, 'BV421-A|FITC-A|BV510', 1)
gs.uncomped <- GatingSet(resQC)
gs.comped <- compensate(gs.uncomped, comp[[2]])
gs.gated[[2]] <- quick_gate(gs.comped)

```

